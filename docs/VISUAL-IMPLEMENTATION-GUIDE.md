# ADMIN DIRECT PRESTASI - VISUAL IMPLEMENTATION GUIDE

## 🎯 BEFORE vs AFTER

### BEFORE (Current - Inefficient)
```
┌─────────────────────────────────────────────────────────┐
│  Admin wants to add new prestasi                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
         ┌─────────────────┐
         │  /submit page   │  ← Public form
         │  (as student)   │
         └────────┬────────┘
                  │
                  ▼
      ┌──────────────────────┐
      │ POST /api/prestasi/  │
      │       submit         │
      │ status='pending'     │
      └──────────┬───────────┘
                 │
                 ▼
      ┌─────────────────────┐
      │ /admin/prestasi     │  ← Review own submission
      │ Click "Approve"     │
      └──────────┬──────────┘
                 │
                 ▼
     ┌────────────────────────┐
     │ PATCH /api/admin/      │
     │   prestasi/[id]        │
     │ Creates Prestasi       │
     └──────────┬─────────────┘
                │
                ▼
      ┌─────────────────┐
      │ Published! ✅    │
      └─────────────────┘
```

### AFTER (New - Efficient)
```
┌─────────────────────────────────────────────────────────┐
│  Admin wants to add new prestasi                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
      ┌───────────────────────┐
      │ /admin/prestasi       │
      │ Click "Buat Prestasi  │
      │        Baru" button   │
      └──────────┬────────────┘
                 │
                 ▼
      ┌────────────────────────┐
      │ /admin/prestasi/create │  ← Admin-only form
      │ Fill form + upload     │     with thumbnail
      │ thumbnail & galeri     │     & galeri
      └──────────┬─────────────┘
                 │
                 ▼
      ┌────────────────────────┐
      │ POST /api/admin/       │
      │   prestasi/direct      │
      │ Creates both:          │
      │ - PrestasiSubmission   │
      │   (submitter='ADMIN')  │
      │ - Prestasi (published) │
      └──────────┬─────────────┘
                 │
                 ▼
      ┌─────────────────┐
      │ Published! ✅    │
      │ Homepage stats   │
      │ auto-updated! 📊 │
      └─────────────────┘
```

---

## 📸 THUMBNAIL & GALERI FLOW

### Upload & Display Journey
```
┌────────────────────────────────────────────────────────┐
│  ADMIN FORM: /admin/prestasi/create                    │
├────────────────────────────────────────────────────────┤
│                                                         │
│  📸 Thumbnail Upload (REQUIRED)                        │
│  ┌──────────────────────────────────┐                 │
│  │  [Drop image or click to upload] │                 │
│  │   - Max 5MB                       │                 │
│  │   - JPG, PNG, WebP                │                 │
│  │   - Recommended: 1200x675px       │                 │
│  └──────────────────────────────────┘                 │
│  Preview: [████████████████]                           │
│                                                         │
│  🖼️ Galeri/Dokumentasi (Optional)                     │
│  ┌──────────────────────────────────┐                 │
│  │  [Drop images or click to upload]│                 │
│  │   - Max 10 images                 │                 │
│  │   - Max 5MB each                  │                 │
│  │   - Drag to reorder               │                 │
│  └──────────────────────────────────┘                 │
│  Preview Grid:                                         │
│  [███] [███] [███]                                     │
│  [███] [███] [███]                                     │
│                                                         │
└────────────────────────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────┐
         │  Upload Process   │
         ├───────────────────┤
         │  1. File selected │
         │  2. POST /api/    │
         │     upload        │
         │  3. Cloudinary    │
         │     upload        │
         │  4. Return URL    │
         │  5. Store in      │
         │     form state    │
         └────────┬──────────┘
                  │
                  ▼
       ┌──────────────────────┐
       │  Form Submit          │
       │  POST /api/admin/     │
       │    prestasi/direct    │
       │                       │
       │  Body:                │
       │  {                    │
       │    thumbnail: "https  │
       │      ://res.cloud...",│
       │    galeri: [          │
       │      "https://...",   │
       │      "https://...",   │
       │    ]                  │
       │  }                    │
       └────────┬──────────────┘
                │
                ▼
     ┌──────────────────────┐
     │  Database Storage    │
     │  apm_prestasi:       │
     │  - thumbnail (text)  │
     │  - galeri (jsonb)    │
     └────────┬─────────────┘
              │
              ▼
┌─────────────────────────────────────────────┐
│  PUBLIC DISPLAY                             │
├─────────────────────────────────────────────┤
│                                             │
│  Homepage (/):                              │
│  ┌─────────────────┐                       │
│  │ [THUMBNAIL]     │  ← Shows thumbnail    │
│  │ Juara 1 ...     │                       │
│  └─────────────────┘                       │
│                                             │
│  List Page (/prestasi):                    │
│  ┌─────────────────┐                       │
│  │ [THUMBNAIL]     │  ← Shows thumbnail    │
│  │ Juara 1 ...     │     in card view      │
│  └─────────────────┘                       │
│                                             │
│  Detail Page (/prestasi/[slug]):           │
│  ┌────────────────────────────┐            │
│  │  [LARGE THUMBNAIL]         │  ← Hero    │
│  │  Hero Image                │            │
│  └────────────────────────────┘            │
│                                             │
│  Dokumentasi Section:                      │
│  ┌────┐ ┌────┐ ┌────┐                     │
│  │[G1]│ │[G2]│ │[G3]│  ← Galeri grid      │
│  └────┘ └────┘ └────┘                     │
│  ┌────┐ ┌────┐ ┌────┐                     │
│  │[G4]│ │[G5]│ │[G6]│                     │
│  └────┘ └────┘ └────┘                     │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 📊 HOMEPAGE STATS AUTO-UPDATE

### How Stats Update Automatically
```
┌────────────────────────────────────────────────┐
│  HOMEPAGE (/app/page.tsx)                      │
│                                                 │
│  const siteStats = await getSiteSettings()     │
│         │                                       │
│         ▼                                       │
│  GET /api/site-settings                        │
│         │                                       │
│         ▼                                       │
│  ┌──────────────────────────────────┐          │
│  │  Calculate Stats from Database   │          │
│  │  ┌────────────────────────────┐  │          │
│  │  │ totalLomba = COUNT(*)      │  │          │
│  │  │   FROM apm_lomba           │  │          │
│  │  │   WHERE is_deleted=false   │  │          │
│  │  │   AND status != 'closed'   │  │          │
│  │  └────────────────────────────┘  │          │
│  │  ┌────────────────────────────┐  │          │
│  │  │ totalPrestasi = COUNT(*)   │  │          │
│  │  │   FROM apm_prestasi        │  │          │
│  │  │   WHERE is_published=true  │  │  ← Auto! │
│  │  └────────────────────────────┘  │          │
│  │  ┌────────────────────────────┐  │          │
│  │  │ totalExpo = COUNT(*)       │  │          │
│  │  │   FROM apm_expo            │  │          │
│  │  │   WHERE is_deleted=false   │  │          │
│  │  └────────────────────────────┘  │          │
│  │  ┌────────────────────────────┐  │          │
│  │  │ totalMahasiswa =           │  │          │
│  │  │   COUNT(DISTINCT nim)      │  │          │
│  │  │   FROM apm_prestasi_       │  │          │
│  │  │     team_members           │  │          │
│  │  └────────────────────────────┘  │          │
│  └──────────────────────────────────┘          │
│         │                                       │
│         ▼                                       │
│  Display on Homepage:                          │
│  ┌────────────────────────────────┐            │
│  │  🏆 Lomba Aktif          25    │            │
│  │  ⭐ Prestasi Tercatat    142   │  ← Real!  │
│  │  👥 Mahasiswa Bergabung  318   │            │
│  │  📅 Expo & Pameran       12    │            │
│  └────────────────────────────────┘            │
└────────────────────────────────────────────────┘

When Admin Creates New Prestasi:
┌────────────────────────────────────┐
│ POST /api/admin/prestasi/direct   │
│ Creates Prestasi (is_published=true)│
└──────────┬─────────────────────────┘
           │
           ▼ Database Updated
┌────────────────────────────────────┐
│ INSERT INTO apm_prestasi           │
│ VALUES (..., is_published=true)    │
└──────────┬─────────────────────────┘
           │
           ▼ Next Homepage Load (within 5min)
┌────────────────────────────────────┐
│ COUNT(*) FROM apm_prestasi         │
│ WHERE is_published=true            │
│ Result: 143 (was 142)  ← +1! ✅    │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│ Display: Prestasi Tercatat   143  │
└────────────────────────────────────┘

✅ NO CODE CHANGES NEEDED FOR STATS!
```

---

## 🗂️ DATABASE RELATIONSHIPS

### Prestasi Data Model
```
┌─────────────────────────────────────────┐
│  apm_prestasi_submissions               │
│  (Submission/Draft state)               │
├─────────────────────────────────────────┤
│  id (PK)                                │
│  judul                                  │
│  nama_lomba                             │
│  tingkat                                │
│  peringkat                              │
│  submitter_name  ← 'Admin' (for admin) │
│  submitter_nim   ← 'ADMIN' (identifier)│
│  status          ← 'approved' (direct) │
│  reviewed_at                            │
│  reviewed_by                            │
└────────┬────────────────────────────────┘
         │ 1:Many
         ├──────────┐
         │          │
         ▼          ▼
┌────────────────┐  ┌──────────────────┐
│ team_members   │  │ documents        │
├────────────────┤  ├──────────────────┤
│ submission_id  │  │ submission_id    │
│ nama           │  │ type (sertifikat,│
│ nim            │  │   dokumentasi,   │
│ is_ketua       │  │   surat)         │
└────────────────┘  │ file_path (URL)  │
                    └──────────────────┘
         │ 1:1 (UNIQUE)
         ▼
┌─────────────────────────────────────────┐
│  apm_prestasi                           │
│  (Published/Public state)               │
├─────────────────────────────────────────┤
│  id (PK)                                │
│  submission_id (FK, UNIQUE) ← Links!    │
│  judul                                  │
│  slug (UNIQUE)                          │
│  nama_lomba                             │
│  tingkat                                │
│  peringkat                              │
│  tahun                                  │
│  thumbnail        ← Cloudinary URL      │
│  galeri (JSONB)   ← Array of URLs      │
│  sertifikat       ← Cloudinary URL      │
│  sertifikat_public                      │
│  is_featured                            │
│  is_published     ← true for public     │
└─────────────────────────────────────────┘
```

### Why Dummy Submission?
```
Admin Direct Create:
  1. Create PrestasiSubmission
     - submitter_nim = 'ADMIN' (identifier)
     - status = 'approved' (skip review)
     - No actual student data
  
  2. Create Prestasi
     - submission_id = [dummy submission ID]
     - All display fields (judul, slug, etc.)
     - thumbnail + galeri URLs
     - is_published = true
  
Benefits:
  ✅ No schema migration (submission_id stays required)
  ✅ Maintains referential integrity
  ✅ Clear audit trail (can identify admin-created)
  ✅ Existing code expects submission_id → works!
```

---

## 🎨 FORM LAYOUT PREVIEW

```
┌────────────────────────────────────────────────────────┐
│  ← Back   Buat Prestasi Baru                           │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ━━━ INFORMASI PRESTASI ━━━                           │
│                                                         │
│  Judul Prestasi *                                      │
│  ┌──────────────────────────────────────────────┐     │
│  │ Juara 1 Gemastik XVI 2026                    │     │
│  └──────────────────────────────────────────────┘     │
│  Slug: juara-1-gemastik-xvi-2026 (auto)              │
│                                                         │
│  Nama Lomba *                                          │
│  ┌──────────────────────────────────────────────┐     │
│  │ Gemastik XVI                                  │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  Penyelenggara                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ Puspresnas Kemdikbudristek                    │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐             │
│  │ Tingkat *       │  │ Peringkat *     │             │
│  │ ▼ Nasional     │  │ ▼ Juara 1      │             │
│  └─────────────────┘  └─────────────────┘             │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐             │
│  │ Tanggal         │  │ Tahun *         │             │
│  │ 2026-08-15     │  │ 2026           │             │
│  └─────────────────┘  └─────────────────┘             │
│                                                         │
│  ━━━ MEDIA (THUMBNAIL & GALERI) ━━━                   │
│                                                         │
│  📸 Thumbnail * (Required)                             │
│  ┌──────────────────────────────────┐                 │
│  │  [Drop image or click]           │                 │
│  │   Recommended: 1200x675px        │                 │
│  └──────────────────────────────────┘                 │
│  Preview: [████████████████]                           │
│                                                         │
│  🖼️ Galeri Dokumentasi (Optional)                     │
│  ┌──────────────────────────────────┐                 │
│  │  [Drop images or click]          │                 │
│  │   Max 10 images, drag to reorder │                 │
│  └──────────────────────────────────┘                 │
│  [███] [███] [███] [+Add more]                        │
│                                                         │
│  📄 Sertifikat (Optional)                              │
│  ┌──────────────────────────────────┐                 │
│  │  [Upload PDF/Image]              │                 │
│  └──────────────────────────────────┘                 │
│  ☑️ Sertifikat dapat dilihat publik                   │
│                                                         │
│  ━━━ DESKRIPSI ━━━                                    │
│  [Rich Text Editor]                                    │
│                                                         │
│  ━━━ TIM MAHASISWA (Optional) ━━━                     │
│  + Tambah Anggota Tim                                  │
│                                                         │
│  ━━━ PEMBIMBING (Optional) ━━━                        │
│  + Tambah Pembimbing                                   │
│                                                         │
│  ━━━ PENGATURAN TAMPILAN ━━━                         │
│  ☐ Tampilkan di Featured                              │
│  ☑️ Publish Langsung                                  │
│  ☑️ Tambahkan ke Kalender                             │
│                                                         │
│  ┌────────────┐  ┌────────────┐  ┌────────┐          │
│  │ 💾 Simpan  │  │ ✅ Publish │  │ Batal  │          │
│  └────────────┘  └────────────┘  └────────┘          │
└────────────────────────────────────────────────────────┘
```

---

## ✅ IMPLEMENTATION CHECKLIST

### Phase 1: Backend API
- [ ] Create `app/api/admin/prestasi/direct/route.ts`
- [ ] Implement POST handler with auth check
- [ ] Create transaction: PrestasiSubmission + Prestasi
- [ ] Validate thumbnail URL (required)
- [ ] Store galeri array in Prestasi.galeri (jsonb)
- [ ] Return success with slug for redirect

### Phase 2: Frontend Form
- [ ] Create `app/admin/prestasi/create/page.tsx`
- [ ] Add ImageUpload for thumbnail (required field)
- [ ] Add ImageUpload for galeri (multiple, optional)
- [ ] Add image preview & reorder functionality
- [ ] Add all other form fields
- [ ] Add validation (thumbnail required!)
- [ ] Add submit handler

### Phase 3: Verify Homepage Stats
- [ ] Create prestasi via new form
- [ ] Wait 5 minutes or clear cache
- [ ] Verify "Prestasi Tercatat" increased by 1
- [ ] Verify database count matches display

### Phase 4: Verify Detail Page
- [ ] Navigate to /prestasi/[slug]
- [ ] Verify thumbnail displays as hero image
- [ ] Verify galeri displays in grid layout
- [ ] Test with 1, 5, and 10 images

### Phase 5: Testing
- [ ] Test form validation (thumbnail required)
- [ ] Test image uploads (size limits)
- [ ] Test with/without galeri
- [ ] Test mobile responsive
- [ ] Test end-to-end workflow

---

**🎯 All visual flows documented!**  
**Ready for implementation!** 🚀
